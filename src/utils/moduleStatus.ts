//TODO: El siguiente código define un objeto `moduleStatus` que contiene el estado de varios módulos en una aplicación, el nombre de la key es el nombre que esta guaradado en el mongo
export const moduleStatus: Record<string, boolean> = {
  'nasaAsteroid': false,
  'nasaGallery': true,
  'contact': false,
  'allAplications': false,
};
